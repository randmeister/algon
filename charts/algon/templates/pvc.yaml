{{- $fullName := include "algon.fullname" . -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: taskname: {{ $fullName }}-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.size }}
      